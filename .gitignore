# node
node_modules/
.DS_Store


# build output
dist/
coverage/
*.tsbuildinfo

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.env
.env.*
!.env.example

# api/swarm/*.js are generated by scripts/bundle-functions.mjs from server/functions/ and
# regenerated on every build. They are committed anyway: on a Git-connected Vercel project
# the `functions` pattern in vercel.json is validated BEFORE the build runs.
.vercel/
